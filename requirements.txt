Flask==3.1.0

demucs==4.0.1
librosa==0.10.2.post1
soundfile==0.12.1
python-dotenv==1.0.1
requests==2.32.3
ffmpeg-python==0.2.0
torch==2.5.1
torchaudio==2.5.1

# Notes:
# - System-level FFmpeg must be installed separately (brew install ffmpeg / apt-get install ffmpeg)
# - Torch installation may require platform-specific wheels; pin versions as needed for your environment
