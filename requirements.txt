Flask==3.1.0
librosa==0.10.2.post1
soundfile==0.12.1
numpy

demucs==4.0.1
# PyTorch and torchaudio are required for Demucs to run efficiently; install platform-specific wheels as needed
torch==2.5.1
torchaudio==2.5.1

# NOTE: System-level FFmpeg is required (install via `brew install ffmpeg` on macOS).
# Remove any packages you do not need for your deployment.

pytest
